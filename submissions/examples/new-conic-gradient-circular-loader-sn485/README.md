# Conic Gradient Circular Loader

A premium CSS-only conic gradient circular loader with concentric breathing border ring.

## How to use
1. Link the component stylesheet `style.css` in your HTML header.
2. Embed the structure code into your body sections.
